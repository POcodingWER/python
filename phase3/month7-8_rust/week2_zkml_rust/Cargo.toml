[package]
name = "zkml_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
# ZK 라이브러리
halo2_proofs = "0.3"
ff = "0.13"
group = "0.13"

# 유틸리티
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# 성능 측정
criterion = "0.5"
